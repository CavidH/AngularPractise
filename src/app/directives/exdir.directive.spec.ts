import { ExdirDirective } from './exdir.directive';

describe('ExdirDirective', () => {
  it('should create an instance', () => {
    const directive = new ExdirDirective();
    expect(directive).toBeTruthy();
  });
});
